# Workflow fixture replies

The isolated workflow transcript uses these explicit operator replies:

- territory map: accepted
- destination Question 1: `1-A`
- round challenge check: no challenge found because the answer matches the stated constraint
- prototype Question 2: `V`
- locks table: repo `akrogon`, priority `n`, ownership `agent-owned`, consult `no`
- series confirmation: `3-A`

The proposal-mode scenarios use no confirmation reply. They must stop at a
proposal seed even when the checkout ground is present.
