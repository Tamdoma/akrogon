# Jul 28 skills (commit 2f1fdbc3): keep / cut

Keep: canonical file model (plan.md, a.md, b.md per phase), blind independent positions,
one rebuttal round, synthesis by the slot that executes (A plans, B implements), fidelity
audit by the other slot, decision IDs D1..Dn, substantive-file rule, phases
P-draft P-synth I-draft I-synth I-ready C-ready C-fix D-merge, `lifecycle.ts phase` and
`transition`, worktree scripts, merge-branch.ts.

Cut: status-write/status-read/status-verdict, pair.yaml, slot-default files, park.hold,
`My last operation` and `Next step` footers, Grounding footer format, exact approval
sentences, brief-grill and position-interview lanes, PowerShell/Windows branches,
advisory-quality-standards AQ- machinery, series-materialization reference (leaf shape now
comes from chart/create-issue), fresh-read rituals (driver supplies phase and step).
