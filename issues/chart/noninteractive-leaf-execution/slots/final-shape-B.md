1. **F1 — One acceptance test requires the downstream leaf.** `failed-with-cause/brief.md:14` requires repeated next calls after stopping to issue no notice, but its design explicitly leaves `failed_notified` untouched (`design.md:25`). `commitMove` resets that flag (`src/phase.ts:29`), and the first next call therefore emits the existing failed notice (`src/next.ts:479-482`). Removal belongs to failure-attention, which depends on this leaf. Move that no-notice assertion to failure-attention, or move notification removal into failed-with-cause. Keep the busy-field/no-prompt assertions here.

2. **F2 — Exit 0 on delivery errors remains an unsupported interpretation.** `failure-attention/brief.md:4` attributes this to the operator, but its quoted binding decision (`design.md:6`) only says the failed state stays visible regardless of delivery. Keeping the persisted transition does not require reporting successful command execution. Preserve failed state and the recorded delivery error, attempt the independent tab rename, then surface the final transport failure. This also follows the supplied error-handling rule to retry with warnings and raise the last external-API error.

3. **Shape:** No simpler complete design is evident than disabling human questions at execution launch, providing one seat-declared stop, and attaching attention to that transition. Removing the generic blocked-event trigger avoids the false stops demonstrated by #21. This remains conditional on completing the declared launch/relaunch rollout, not merely shipping the three leaves.
