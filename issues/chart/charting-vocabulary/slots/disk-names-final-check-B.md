- **F1 — Migration omits the current chart.** `issues/chart/charting-vocabulary/CHART.md` and its three `decisions/*.md` files already use the old contract. Migrating only `status-empty-open` and closed charts leaves this chart reporting zero forks after `src/status.ts:241–246` switches formats. Include every existing active chart at migration time, including this one, and update its working links. Retain the current close-time move unchanged.

- **F2 — Visible status vocabulary remains inconsistent.** `src/status.ts:217` also prints `DECIDED`, which the contract leaves unchanged. `docs/guide/in-practice.html:75` describes the output as “decided/total decisions, unspecified items,” and `docs/guide/create.html:58` describes a chart of decisions settled one by one. Include these specific wording surfaces, with `TAKEN` for the status count header and taken/total forks plus fog in its explanation. Updating only `files.html:106` misses the direct status documentation.

- **F3 — The operator's “fork decision” term is missing.** The quoted correction explicitly calls what was previously a fork a “fork decision.” The proposed vocabulary defines topic-level forks and questions but does not explain that term. Define a fork decision as the individual choice addressed by a Q block, within the topic-level fork, or obtain clarification if that is not the intended relationship. Keep the existing detail and recommendation for every Q block.

- **F4 — “When the operator answers” is too broad without the existing answer rules.** `skills/chart-issues/assets/questions.md` distinguishes a request for explanation from an explicit choice and keeps omitted material answers open. Preserve those rules explicitly when defining when a question or fork decision is taken. A reply asking “why A?” must not produce `## Taken`. Continuous Q numbering and presentation of all currently material questions must also survive the new topic grouping.
