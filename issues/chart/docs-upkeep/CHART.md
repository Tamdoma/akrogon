# Chart: docs-upkeep

## Destination
Every merged leaf leaves the agent docs and human docs describing the code as it now is: plan names the affected docs, implement updates them, check verifies them from the changed behavior. `akrogon init` cannot register a repo whose declared index is missing or empty, and grounding is never `none` by accident. The stale indexes in pi-extensions and mdcny are repaired.

## Forks taken
- [round-1](forks/round-1.md): Q1-A plan names affected docs; Q2-A check verifies from changed behavior and may open unchanged pages; Q3-A repair pi-extensions and mdcny indexes as leaves; Q4-A init-issues skill maps, `akrogon init` validates the declared index, no silent `none`.

## Fog
None.

## Off route
- Boulevard re-grounding: operator runs init-issues on that repo after L1 merges. Not a leaf.
- akrogon watch-issues doc rows: written directly on main 2026-09-19 by operator instruction.
- Framework AREA shorthand paths: not deletions, no change.
- grounding.docs, surfaces, indexed_scopes: unread fields stay as they are; no new consumer.
- Watchers, hooks or programs that edit docs: excluded by operator lock.

Handed off 2026-09-19
