# Chart: fork-rounds

## Destination
The chart-issues door takes forks one at a time again: each fork gets its own researched operator round, the remaining forks and fog are reshaped after every answer, and a one-fork destination still costs one round. Same vocabulary as today (forks, taken, fog, off route). Minimal wording change in the skill.

## Forks taken
- [round-granularity](forks/round-granularity.md): one fork per round, research redone from the answers so far, rest reshaped after each answer (2026-09-21).
- [fork-order](forks/fork-order.md): CHART.md holds an ordered Open forks list, re-sorted after each answer, no new field; shapes.md line 77 changes to allow it (2026-09-21).
- [fog-graduation](forks/fog-graduation.md): after each answer, re-read Fog, move newly sharp questions into fork files, reshape and re-sort, then research the next fork; written as a step in Take (2026-09-21).

## Fog
None. Every open question is in forks/.

## Off route
- Restoring the 2026-09-08 decision file fields (`Status`, `Type`, `blocked-by`, claims). The command has no chart state and the operator asked for minimal change.
- Restoring one decision per session. Sessions were the old unit because the skill ran unattended lanes; the door is one attended pass with many rounds and resumes from CHART.md after compaction.
- Changing the round shape in `assets/questions.md` (opening paragraph, research line, options, pitfalls, challenge check). It survived the rewrite unchanged.

Handed off 2026-09-21
