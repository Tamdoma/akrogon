# Chart: the phase skills name one env-file rule

## Destination
Each of the four phase skills (plan, implement, check, merge) and `skills/AREA.md` states, in one sentence, that a seat never opens, prints or writes the operator env file with a tool; values are loaded only by `bun --env-file=<file> <script>`, scripts print results and never values, and a variable's presence is checked the same way by name. The plan skill's credential check becomes that by-name check. Nothing else in the skills changes.

## Forks taken
None needed: the map found no fog for this half. The four answers that shape the enforcement half are recorded in the pi-extensions fork `guard-scope.md`.

## Fog
None.

## Off route
- Refusal and scrubbing inside pi's tools: pi-extensions leaf `env-guard-hooks` (handed off 2026-09-19).
- Rotating the leaked credential: operator decision on #22, not rotated.
- Any akrogon code change: none required; `akrogon phase <slug> failed --reason ... --slot` already carries a missing-value stop (skills-never-ask, merged).

## Territory findings
See `slots/map-merged.md`.

Handed off 2026-09-19
