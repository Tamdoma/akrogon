# Merged round: artifact placement (2026-09-18)

Q1 gate at every worktree move: (both) yes, split the guard, issues-diff refusal at every `phase` call with a recorded worktree, empty-branch refusal only when the destination is check.review; run before any slot is recorded (src/phase.ts:121-128); message names the authoritative folder and offending paths. (B) applies to failed recovery with a worktree too; scope to `akrogon phase`, not the dispatch failure path via commitMove (src/next.ts:380). (A) leaves before first allocate have no worktree, so no check.
Q2 path in prompt: (both) yes, append the resolved absolute authoritative leaf folder to every dispatched prompt and make each skill's write lines resolve pass artifacts there while code edits use the worktree. (B) check-issue :14,33, merge-issue :27,31 and implement-issue :31,39 need the same line, and manual skill use keeps the config lookup. (A) the prompt contract line in each skill changes together.
Q3 artifact existence gate: (both) no, keep today's debate gate only; existence checking is a separate behavior with rebuttal-off, debate-off and failed-recovery variants.
Recommendation (both): 1-A 2-A 3-A.
