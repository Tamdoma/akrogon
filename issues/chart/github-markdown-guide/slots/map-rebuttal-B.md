1. There are **four Playwright configurations**, not three. Retirement must include `tests/browser/playwright.config.ts:1-14` plus `docs-concepts.config.ts:1-7`, `docs-operate.config.ts:1-8`, and `docs-practice.config.ts:1-8`. All import `@playwright/test`. Leaving one while removing the dependency leaves an orphaned import. Correct both the surface inventory and the same-leaf deletion scope.

2. “Settled without asking (both)” misstates B's position. `map-B.md` explicitly says its recommendations are not operator decisions, and Q4 presents immediate HTML retirement versus a compatibility window as an open fork, with P3 asking whether saved local bookmarks matter. I recommend immediate retirement, but did not mark it settled. Label this a joint recommendation unless an operator answer has actually selected it. The chart convention distinguishes recommendation from settlement: `/home/ivan/.codex/skills/chart-issues/assets/questions.md`, under the question shape, says recommendations and silence cannot supply an operator answer.
