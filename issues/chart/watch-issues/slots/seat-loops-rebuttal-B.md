1. **Q3-A does not create a fresh seat.** `phase failed` followed by recovery resets lifecycle fields but preserves pane/worktree identity (`src/phase.ts:95-112`). `next` starts an agent only when `pane.agent === null` (`src/next.ts:373-389`). Esc aborts a turn without inherently replacing the pi session. Replace “fresh seat” with “same existing seat/session, restarted phase,” or explicitly identify and authorize a separate session-replacement mechanism. Re-prompting the same looping context is not a proven reset.

2. **Q3-A's escalation condition needs evidence of post-steer execution, not merely another fire showing repetition.** Pi queues steering until current tool calls finish (`/home/ivan/.pi/agent/extensions/tamdoma-subagents/node_modules/@earendil-works/pi-coding-agent/docs/sdk.md:224-234`). The second screen may show pre-steer output or a correction still queued. Require confirmed consumption and new repeated completed actions after that correction before describing it as ineffective. Q2's receipt check must be an explicit prerequisite to Q3, not an assumption based on elapsed time.

3. **Q3-A must request an explicit cancellation exception, not redefine “working.”** Confirmed repetition does not prove every process in the turn is unproductive. Pi's abort handler kills the process tree (`/home/ivan/.pi/agent/extensions/tamdoma-subagents/node_modules/@earendil-works/pi-coding-agent/dist/core/tools/bash.js:64-86`), and parallel tool execution is supported (`docs/extensions.md:655-659` in the same package). The proposed Esc can cancel productive concurrent work. I continue to recommend Q3-B. If A remains an option, state plainly that choosing it relaxes the no-kill lock and accepts that collateral cancellation risk; two observations and one steer cannot guarantee otherwise. Existing recovery must still wait for all relevant seats to become quiescent after any authorized interruption.
