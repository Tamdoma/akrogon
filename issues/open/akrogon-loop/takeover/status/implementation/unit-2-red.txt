bun test v1.4.0 (34cbb9a40)

tests/next.test.ts:
plan-issue build slot=B phase=plan.synthesis
(pass) next creates one worktree/tab under concurrent hooks, prompts configured B, ignores working events and resolves hook cwd [532.82ms]
77 |     expect(readFileSync(resolve(f.root, 'issues/log.jsonl'), 'utf8')).toContain('"to":"failed"');
78 |     const state: string = readFileSync(resolve(path, 'state.yaml'), 'utf8');
79 |     const before: number = calls(f).length;
80 |     expect((await next(f, ['retry'])).code).toBe(0);
81 |     const notified: string[][] = calls(f).slice(before);
82 |     expect(notified).toHaveLength(1);
                          ^
error: expect(received).toHaveLength(expected)

Expected length: 1
Received length: 0

      at <anonymous> (/home/ivan/Work/infra/akrogon/issues/worktrees/status/tests/next.test.ts:82:22)
(fail) next resumes interrupted tab creation, retries same slot twice then peer once and fails [639.30ms]
94 |   try {
95 |     const path: string = leaf(f, 'broken', 'failed');
96 |     const state: string = readFileSync(resolve(path, 'state.yaml'), 'utf8');
97 |     const notified: Result = await next(f, ['broken']);
98 |     expect(notified.code).toBe(0);
99 |     expect(calls(f)).toHaveLength(1);
                          ^
error: expect(received).toHaveLength(expected)

Expected length: 1
Received length: 0

      at <anonymous> (/home/ivan/Work/infra/akrogon/issues/worktrees/status/tests/next.test.ts:99:22)
(fail) next notifies failed leaves without dispatch or state changes and exposes notification failures [45.22ms]
(pass) next refuses hand-built and dependencies, respects capacity, and sends unknown panes to idle peers [620.93ms]
(pass) merged phase leaves tab intact, next closes it and starts dependent from a closed-folder hook [681.49ms]
(pass) machine-wide capacity includes another registered repo and folder selection respects it [358.35ms]
(pass) next recovers only merge-phase work by ancestry against a non-default remote target [459.53ms]
(pass) exited hooks resolve persisted pane hints and preserve the surviving slot [623.64ms]
(pass) delayed working notifications never consume retries and idle notifications still dispatch [488.35ms]
(pass) a live merge retains its completion call after pushing, including a peer retry [476.62ms]
(pass) agent names support identical repo-local slugs and long numeric-leading slugs [496.19ms]

 9 pass
 2 fail
 81 expect() calls
Ran 11 tests across 1 file. [5.46s]
