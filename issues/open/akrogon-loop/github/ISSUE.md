# Issue: github

Chart skill version: 4

The GitHub intake path: the pull mirror with closure on completion, and the seed-issue skill that colleagues' agents run. Two parallel leaves after takeover.

## Leaves

- `pull-close`: `akrogon pull` and `--all`, the seeds mirror, GitHub close on container completion, startup pull.
- `seed-issue`: The seed-issue skill: posts a report to GitHub with gh, target from the routing file or the origin.
