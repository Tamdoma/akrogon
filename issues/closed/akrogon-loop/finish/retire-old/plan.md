# Plan: retire-old

Direct synthesis from brief/design because state.yaml has debate: no. No positions or rebuttals are expected. No new dependency: the existing blocked-by list already orders this final retirement after the replacement leaves.

## Read first

- This leaf's brief.md and design.md in the authoritative issues/open/akrogon-loop/finish/retire-old directory.
- README.md and package.json for the documentation replacement and actual check commands.
- skills/init-issues/SKILL.md for the authorized setup procedure.
- src/init.ts and src/config.ts for initialization, registration and effective-config behavior; src/akrogon.ts and src/install.ts for documented command interfaces.
- tests/init.test.ts and tests/helpers.ts for existing initialization scenarios.
- learnings/LESSONS.md was read during planning as evidence, not instructions. The cited dangling-link case was checked in learnings/history/2026-09-10-core-skills.md. Workers do not need to reread lesson history as guidance.

Grounding gap: effective configuration currently says grounding: none. There is no configured index to read. D3 supplies it.

## Decisions

- **D1. Preserve archives mechanically.** Move every file below reference/lessons into learnings/history with the same relative suffix and unchanged bytes. Current inventory has 19 files, including README.md and two JSON files. Capture the actual source manifest before moving and reject destination collisions rather than overwrite existing history. Delete the remaining reference and new-beginning trees only after inbound-reference review. Do not derive active lessons from the archive.
- **D2. Repair navigation, preserve records.** Inspect Markdown links, reference-style link definitions and plain actionable file paths in learnings, skills, README.md and authoritative open leaves, resolving relative paths from each containing file. Retarget moved-lesson references to their new location. Replace instructions that direct readers to deleted machinery with the actual current entry point, or remove obsolete navigation when no replacement exists. A sentence describing a deletion, a previous result, or locked historical design is not navigation. Keep creation-locked design verbatim, issues/chart untouched, and old and moved history byte-identical. Do not use a zero-match grep criterion that rejects these required records.
- **D3. Add one small index.** Write REFERENCE.md with one line per area linking directly to existing folders: command (src/), tests (tests/), skills (skills/), integration (plugin/), documentation (docs/), issue records (issues/) and learnings (learnings/). No area documents, per-file inventory or index parser. Keep # Lessons and existing active lines in learnings/LESSONS.md, adding the short explanation that these record what happened, not what is true.
- **D4. Use init for configuration.** Invoke the init-issues skill during implementation, with this leaf providing authorization. Preserve all inspected effective choices and both broadcast environment-variable names. Set grounding.index to REFERENCE.md through a complete temporary proposal and the installed akrogon init --from command. Follow the skill's established-suite requirement for checks.test_changed, verifying the installed runner before selecting its supported command or the documented required-base/full-suite command. Do not add a toolkit or change dependencies. Initialization must run from the registered root /home/ivan/Work/infra/akrogon, not this worktree: initialize currently registers basename(git --show-toplevel), so running here would add retire-old and make currentRepo reject duplicate registrations. Copy the command-generated issues/config.yaml into this worktree for the implementation commit, preserving unrelated root changes. Do not hand-edit either config. The root config mutation is an authorized setup result and must be recorded in the implementation report.
- **D5. Document the current product only.** Rewrite README.md around install, init, the command and the eight skills. Ground prerequisites and installation in current source: Bun and checkout dependencies, installed herdr and gh where required, flock, the first source invocation bun src/akrogon.ts install, then installed commands. Explain initialization through init-issues and akrogon init --from, config inspection, and actual install/init/config/phase/next/pull/status interfaces. Link and briefly describe exactly chart-issues, plan-issue, implement-issue, check-issue, merge-issue, seed-issue, broadcast-issue and init-issues. Do not run install merely to verify prose or alter machine skill links.
- **D6. Keep scope mechanical.** The live skills directory already contains exactly those eight folders. Recheck before deleting anything and remove only unexpected retired skill folders. Command and skill behavior, docs/guide.html, KICKOFF.md and archived charts are outside scope except the specifically authorized skill link repairs. No new permanent audit script or tests that only assert file names or wording.

## Acceptance criteria

- **C1.** Neither retired tree exists. The skills directory contains the eight expected folders. Every source-manifest entry has a destination with the same relative suffix and SHA-256, and all pre-existing history remains unchanged.
- **C2.** No surviving actionable Markdown link or path in the specified audit scope resolves into a deleted tree. Report each remaining search match as repaired navigation or preserved record text with its reason. Moved history contents and locked text remain unchanged.
- **C3.** REFERENCE.md has one linked folder per area and all targets exist in the implementation checkout. LESSONS.md retains its heading and every pre-existing active line, adds the required meaning, and gains no lesson distilled from old archives.
- **C4.** The installed init command succeeds against the registered repo. Saved effective-config output identifies repo akrogon and grounding.index: REFERENCE.md. Registration has no new worktree entry. Generated repo configuration is included in the worktree change, preserving prior choices except grounding and the init skill's verified test_changed addition. Manifest, lockfile and unrelated global settings remain unchanged.
- **C5.** README accurately describes the installed interfaces and eight current skills, with working links and no retired operating instructions. Verification does not invoke real GitHub, panes, installation roots or the herdr socket as a test.
- **C6.** bun test, bun run typecheck, bun run format and the chosen test_changed command with the real leaf base succeed. Formatting creates no unrelated changes. The implementation report records commands, exit codes, artifact paths and actual limitations.

## Ordered execution checklist

1. **A1 — Snapshot and audit (C1–C3).** Record file hashes and relative suffixes for the old lesson subtree, existing history and active lesson lines. Inventory surviving skills and both the worktree and authoritative open-leaf references. Retain a compact manifest and reference classification as implementation evidence. Check for collisions before the first move. Resolve link targets rather than assuming every old-name match is a broken pointer.
2. **A2 — Move, repair, delete (C1–C2).** Move the old lesson files without transforming contents, repair any actual inbound navigation in owned files, then remove the two retired trees and any extra retired skill directories. Make tracked open-leaf repairs in the worktree and keep necessary authoritative handoff navigation accurate without editing state or locked contracts.
3. **A3 — Write current docs (C3, C5).** Write REFERENCE.md and README.md, and add only the required explanatory header text to LESSONS.md. Validate each Markdown destination against the post-removal checkout. Preserve all prior active lines and history.
4. **A4 — Initialize (C4).** Read/apply the init-issues skill and show its concrete proposal. Run initialization from the registered root using a proposal outside the repo, then copy its generated repo config into the implementation worktree. Remove the temporary proposal on success or failure. Run akrogon config as this leaf's separate acceptance invocation and save output under implementation/verification, alongside init exit status and a registration/settings preservation check. Validate index targets in the worktree. Do not register the worktree or change initialization code.
5. **A5 — Verify and hand off (C1–C6).** Compare every moved file against the pre-move manifest and compare prior history and lesson lines. Repeat the scoped navigation audit including relative and absolute targets. Run the configured checks and the selected test_changed invocation with AKROGON_BASE from the actual merge base. Save concise evidence under implementation/, remove iteration-created helpers, review the final diff for ownership, and commit implementation before check handoff.

## Concrete verification and limitations

For the preservation scenario, the former lesson README and both JSON files must compare equal to their captured hashes at the new paths, just like every Markdown lesson. For the collision edge case, inspect the complete source/destination mapping before moving; a collision is a visible blocker, never an overwrite. For navigation, an inbound link to a moved lesson must open the new file, while a locked sentence saying the old folder will be deleted remains record text. These are manual evidence checks, not brittle permanent wording tests.

Use the existing temp-repo initialization suite for repeat-init preservation and invalid-proposal rejection. Run bun test and bun run typecheck from this worktree, plus configured formatting and the verified changed-test command. Capture the real authorized init/config invocation as the end-to-end configuration artifact. No source behavior changes are planned, so no new command test is needed unless implementation exposes an in-scope functional change.

**R1.** Init updates the canonical tracked config before the implementation branch merges. REFERENCE.md exists in the worktree until merge, so the canonical relative index target has a temporary availability gap. Record this explicitly and ensure integration carries the generated config and index together without losing the already-applied config. Do not pretend the root index was verified before it exists or leave temporary index copies that obstruct merge.

**R2.** Old archive contents and locked design retain historical names by contract. KICKOFF.md and docs/guide.html may also describe retired behavior outside this leaf's ownership. Report concrete findings there without expanding this leaf into their rewrite.

**R3.** The required concurrent-run signal comes from existing github/doors logs. This cleanup cannot manufacture that historical evidence; report missing evidence if unavailable, without running extra lifecycle work.
