# One Door Skill

Chart skill version: 4

Status: resolved
Type: grilling

## Question

Do chart-issues and create-issue stay two skills, or does one skill serve both the territory-sized effort and the single fuzzy issue?

Operator note, 2026-09-08: everything learned in the chart process applies to create-issue too, slot B blind at every step, plain-language questions, learnings kept. The difference is size only. Maybe one skill is better than two.

Constraints already locked: operator only at the two doors. # Multi Chart Layout gives one subfolder per chart. Chart Lane step 3 already says: if the map surfaces nothing unspecified, write no chart, the work fits one session.

## Findings

## Resolution

One skill. chart-issues serves the single fuzzy issue as well: when the territory map surfaces nothing unspecified, the session goes straight to handoff and writes the issue instead of stopping. One protocol, one install, one set of learnings, the same slot B check on every issue. Forecloses a separate create-issue rewrite; # Skill Rewrite now covers one skill. Operator picked it 2026-09-08 from slot A's recommendation, both slots not grilled on it.
