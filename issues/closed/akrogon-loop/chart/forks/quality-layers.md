# Quality Layers

Chart skill version: 4

Status: resolved
Type: grilling

## Question

Whether the fidelity audit and the QA reviewer subagent stay is open, the intake calls keeping them advice (225). Does the advisory pass become the repo's real linter and complexity rules run inside the leaf, blocking or advising by repo config, plus one paragraph of judgement in check-issue, with the AQ- id machinery gone? And ponytail: the 56-line ruleset or the seven-word prompt, in the implementer's read, the checker's, or both?

Coverage pass 2026-09-08 adds: the intake says nothing else in the harness cares beyond the repo's own checks, so judge passes, proof formats and coverage review are gone unless this decision brings one back (intake 69, 71).

Reshape 2026-09-08 after # Debate Count, # Turn Within Phase, # Driver State: check is `check.review` with two `--verdict` flags and a `check.fix` loop capped by config; the QA reviewer sub-pass never had a token and now either lives inside `check.fix` as a subagent the implementer runs or is gone, this decision says which.

Operator note 2026-09-08: when this decision settles which checks run in the leaf, take test impact analysis into account as an option, never a requirement. Test creation itself is in Not Yet Specified until this decision says whether it belongs here.

## Findings

Slot A research 2026-09-08. Fidelity audit: consult-issue/SKILL.md:198-206 gate with two-drift operator escalation (384), the park pattern behind intake 68-69; after # Debate Count the rebuttal is already slot A's read of the plan. QA subagent: implement-issue/SKILL.md:156-180 had it own checklist bookkeeping and AQ- exceedances, prose a later pass reads. Advisory pass: intake 69 measured deterministic checks at 2.2 stall hours per month against 74.5 for everything that watched the agent (tier 2); Google eng-practices, approve when the change improves code health, style is "Nit:" (tier 1); advisory-quality-standards.md:145 counts complexity by hand, a model imitating a tool. Ponytail: the short prompt was the only arm that dropped a security guard (intake 241, InfoQ rebuilt benchmark tier 2), the ladder took the date picker from 404 to 23 lines (243); Shiryaev, JetBrains, 80 tasks, it works only in front of the model every turn, never as a self-activated skill (tier 1). Tests: Willison tier 1, testing cannot be outsourced but the LLM writes the tests; arXiv 2602.07900 tier 2, unguided agents favor prints over assertions; Beck test desiderata tier 1. TIA: Ekstazi 32% average, 54% on long suites, 615 revisions of 32 projects (Gligoric 2015 tier 2); Memon 2017 Google runs only dependency-affected targets (tier 2); Fowler pyramid tier 1; TIA misses config and env changes so a full run stays before handoff.

Slot B round 2026-09-08 (blind): keep a focused fidelity audit (Manshreck and Sadowski tier 1, another reader, cost not established); remove the mandatory QA subagent (Schluntz and Zhang tier 1); repository analyzers measure, reviewers judge, AQ- ids gone (Sadowski static analysis tier 1); repairs required for configured blocking checks and concrete correctness, contract or maintainability defects, preferences advisory; TIA optional and repo-owned, full suite default (Hammant tier 1, Microsoft docs tier 2); reviewers inspect recorded check results and rerun only on a concern; add behavior-focused tests for changed logic and regressions; Ponytail fuller ladder as local text, current file is 28 lines not 56; read by both implementer and checker. Challenge: removing mandatory QA may leave a defect class unchecked with cheap implementers; TIA earns its place only with reliable dependency evidence.

Merge note: the slots disagree on the fidelity audit only, both options offered. All else agrees.

Operator answers 2026-09-08, round 1: 1-A (asks which phase the audit would have been; answer: a plan.audit phase between plan.synth and implement, which now does not exist). 2-A. 3 open: if this covers code quality we need a non-deterministic advisory way to push best practices to the agents, ponytail-like, not installed, no added complexity, to be worked out with slot B. 4-A with deslopifying and simplifying ideas as advice, few rules, no endless fix loop. 5-A, asks how to re-check without an endless check-fix cycle. 6-A, asks what research says about agents writing tests they know will pass, and warns against endless pointless tests, the old repo's downfall. 7-A. 8-A, asks how ponytail is implemented, advisory only, no fix loop, good enough over expensive, but code elegance matters. 9-A.

Slot A research round 2, 2026-09-08. Advice delivery: ponytail AGENTS.md is 32 lines, last changed 2026-07-10 (tier 2); Shiryaev JetBrains July 2026 (tier 1), 80 tasks, -15.4% code, -10.3% cost, p=0.004, zero self-activation as a passive skill; InfoQ 2026-08-05 (tier 2) seven-word prompt dropped a path-traversal guard, ponytail none; Anthropic best practices 2026 (tier 2), bloated instruction files get ignored. Deslopify: no 2026 source measures a post-hoc simplify pass; Anthropic removed the fixing half of /simplify on 2026-05-21 (2.1.147); reviewers asked to find gaps find them. Loop bound: Google eng-practices (tier 1) approve with nits; SWE-Review arXiv 2607.06065 July 2026 (tier 2) cap 5, gains shrink with stronger implementers; GitHub Fix with Copilot May 2026 (tier 2) one fix then re-request. Test gaming: Claude 3.7 card 2025 (tier 2) special-casing tests after repeated failures; METR 2025 (tier 2) o3 hacks 30% of runs; arXiv 2602.00409 MSR 2026 (tier 2) 36% of agent commits add mocks vs 26% human; Wang and Pradel ICSE 2026 (tier 2) 7.8% of solved SWE-bench patches fail developer tests; arXiv 2506.02954 (tier 2) mutation feedback 53 to 89.5% mutation score; Willison 2026-02-24 (tier 1) red-green, tests after code validate bugs. Test bloat: no measured study, only vendor blogs (tier 3); ponytail's own rule, one runnable check, trivial one-liners need no test.

Slot B round 2, 2026-09-08 (blind): advice guides existing passes, no required paragraph (Shiryaev July 2026 tier 1); implementer adopts local simplifications before handoff, after handoff only optional suggestions (Willison March 2026 tier 1); repair review covers the repair diff and earlier findings, not a full re-review (Willison Aug 2026 tier 1, PHPUnit Bergmann Aug 2026 tier 1 keeps the full suite before merge); bug-fix tests fail on old code then pass (Willison Feb 2026 tier 1, Chen et al April 2026 tier 2, volume does not help); one short local reference in the skill source read by both skills. Challenge: a failing-then-passing test can encode a wrong requirement, and the advice must stay optional in practice.

Merge note round 2: both slots agree on every point. Slot A adds the criteria-first test rule and the four blocking items from ponytail's never-cut list.

Operator answers 2026-09-08, round 2: 1-A (asked why "Nit", answer: the Google convention every model knows, the flag does the work). 2-A. 3-A (asked for a guarantee against fix loops, answer: the fix_rounds cap is the guarantee, the review rules only shrink each round, cap is one config number). 4-A (asked what option C is, answer: mutation testing, a repo-optional config command, not by default). 5-A (asked whether the four blocking items can loop or stall, answer: no, bounded by the same cap, failed is a fact not a park). No challenge raised.

Reshape 2026-09-08: # Implementer Brief widened (criteria written by the planner, ponytail file as required reading), # Config Shape widened (check commands, test, test_changed, optional mutation command, quality blocking flag, fix_rounds cap), # Skill Rewrite widened (checker verdict ready-with-nits, no audit, no QA mandate), # Model Tiering widened (re-check is small), # Status View widened (failed after cap must show). Test creation left Not Yet Specified. No fork.

## Taken

Four layers become two. The plan audit and the mandatory QA subagent are gone; the implementer may spawn a subagent freely and nothing records it. The repo's own tools measure: config lists the check commands, linter, typecheck, full tests, an optional changed-tests command, and optionally a mutation-testing command; a quality blocking flag says whether failures block. Reviewers judge: a finding blocks only for a real defect, wrong behavior, broken contract, a concrete maintainability problem, or one of ponytail's four never-cut items when it is also a defect; everything else is a "Nit:" and never starts a fix round. Verdicts are ready, ready with nits, or fix. A re-check reads the repair diff only, confirms earlier findings, and may add a blocking finding only if the repair introduced it; fix rounds are capped by config, three by default, then failed. Reviewers rerun checks only on a change, missing evidence or a concern. Tests: the planner writes acceptance criteria in the brief, the implementer derives tests from them before code, red then green, one per criterion, a fail-first test for a bug, no test for trivial one-liners; the checker compares tests to criteria and rejects mocks of the unit under test. Code-quality advice is one 32-line file, ponytail's rule text, in the skills folder, required reading for implement and check, no plugin, no separate simplify pass. Why: the intake measured the watchers at 74 stall hours a month against 2 for deterministic checks; Shiryaev July 2026 measured rules-in-context working and passive skills never activating; 2025 and 2026 evidence shows agents special-case tests after failures and over-mock, and criteria written by another role before code remove the target. Forecloses: the fidelity audit phase, a mandated QA pass, AQ- ids and the advisory rulebook, a required quality paragraph, a post-hoc simplify pass, full re-review per round, any test selection logic in akrogon, an installed ponytail plugin.

Operator addition 2026-09-08: before a leaf is marked failed at the fix-round cap, slot A writes one paragraph into the plan, what kept failing and why. Prose for the operator only, nothing reads it. On the operator's follow-up about swapping the implementer at the cap: rejected, the reviewer would become author and judge, three failures usually mean a wrong brief or design, and it adds a second counter. Failed is terminal like merged, the operator restarts with `akrogon phase <slug> implement` after fixing the brief, or drops the leaf.

Operator 2026-09-08, later reply: round 2 Q5 answered in words, "understood, I agree", recorded as 5-A. TIA clarified: always used where the repo's config names a changed-tests command, never required, never selected by akrogon; sub-briefs each run the fast command, B runs the full suite once.

Operator-locked 2026-09-08: the brief's commands section lists `test_changed` for the worker and nothing else; workers never run the full suite; the full suite runs once per leaf, by B after the last worker, before handoff; reviewers rerun it only on a specific concern. Config names `test_changed` wherever the runner supports it, which is every repo surveyed.

Reshape 2026-09-08 after # Parallel Merge: check.fix is also entered from merge on conflict or red checks after rebase, with findings written by slot A; at fix round three slot B repairs itself, no worker, before failed (operator 4-yes).

Reshape 2026-09-08 after # Config Shape: the `quality.blocking` flag is gone; every command under `checks` blocks, an optional `advisory` list reports failures as Nit. Slot B repairs itself on the last allowed round, workers take the rounds before it.

From # Index Levels 2026-09-09: the checker verifies the implementer's docs and index lines against the diff; the reviewer never authors them; drift audit script retired.

From # Command Tests 2026-09-09: in the akrogon repo, check-issue treats a test that asserts prose or output wording as a maintainability defect; exact text is testable only where it runs as written; a new test needs a contract or an observed defect behind it, never coverage.

Operator 2026-09-10 (1-A, nits from A's position): at check.review slot A reads its own positions-A.md and rebuttal-A.md first; they say where to look hardest. A finding grounded in a done criterion, a failing `checks` command or a reproducible defect is a Fix and cites which. A finding grounded only in A's own position is a Nit with its reason, never a Fix, so it cannot open a check.fix round. At merge, a Nit slot A still holds and finds reusable becomes one lesson line plus its history file under the # Lessons rule; B meets it at its next plan. No new turn, no new file, no B reply on the same leaf.
