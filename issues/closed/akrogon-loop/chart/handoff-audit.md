# Pre-handoff audit record

Chart skill version: 4

Two passes over the eight leaf contracts on 2026-09-09, before emission into `../../open/akrogon-loop/`. Findings are listed as applied; the staging tree was regenerated after each pass.

## Pass 1: eight dry-run subagents, one per leaf

- command: notification on failed moved to leaf status; seeds gitignore owned by init; one named place in the merged transition for pull-close's close call; install symlinks whatever skill folders exist and prints the removal lines for the old script and skill folders; phase call shape `--slot` and `--verdict`; compare-and-set defined by legal predecessors incl. failed → implement; log fields named; hook resolves the pane cwd through herdr; auto-approval flags in the config launch lines; leaf worktree created on first dispatch; `akrogon config` prints AKROGON_BASE inside a worktree; `akrogon init --from --toolkit`; `AKROGON_HOME` for tests; fixed leaf file names.
- core-skills: phase call shape; AKROGON_BASE from `akrogon config`; ponytail source named; synthesis alone when debate is no; broadcast target from `akrogon config`; missing clauses added (diagnosis paragraph, last round B repairs itself, mismatch return, red-suite sub-brief, read-first list, ancestry check, same-line index conflict, backup pane read, scenario-test rule, must/never with exception, Pocock lines); criterion 5 verified by text; checker is the operator on hand-built leaves.
- status: owns the failed branch in `src/next.ts`; README row dropped; attempts per slot; tab from state; criterion 1 split into readable and unreadable cases.
- pull-close: owns the close call inside the issue lock; startup line is a manifest line with a file-shape test; mirror by number; `--all` continues and exits non-zero; comment commit defined; leaf without `sources` skipped; no new config key; gitignore criterion dropped.
- seed-issue: routing file is an external framework contract; malformed defined; `.claude/` named historical; report shape fixed; depends on gh only; door-external footer; retired references removed; `-R` criterion made concrete.
- chart-issues: criterion 4 scoped to owned skills; `hand_built` only when true; dry run through `AKROGON_HOME`; fixtures, scripts, tests deleted; legacy seeds defined; pull failure warns and continues; a single fuzzy issue still writes a chart folder; door clauses added.
- init-issues: toolkits through `akrogon init --toolkit`; proposal through `--from`; payload and scripts deleted; writes the top index file only; reads global config through `akrogon config`; footer `Next: none`; area files deferred.
- retire-old: criterion 1 scoped to resolving links; eight skill folders; grounding.index set by init-issues then `akrogon init --from`; lessons README and json moved unchanged.

## Pass 2: slot B final contract check, 13 findings, 12 applied

Accepted and applied to the staging tree:
1. (B) Two-slot phases: `akrogon phase` is one atomic op, records the slot in `done`, advances when every required slot is done, prints `recorded` or `moved <phase>`.
2. (B) fix_rounds increments on entry to check.fix; a `fix` verdict past the cap prints `moved failed`; check-issue writes the diagnosis on that output.
3. (B) `phase merged` prints `issue complete <slug>` from the transition that merged the issue's last leaf, separately from any folder move.
4. (B) Tab closure moves from `phase merged` to `next` (from the hook, after the merge pass ended).
5. (B) Two-slot phases get slot files: positions-A/B.md, rebuttal-A/B.md, review-A/B.md; plan.md is the synthesis.
6. (B) `akrogon init` creates learnings/LESSONS.md with the header when absent; retire-old keeps active lines.
7. (B) ponytail.md copied into the core-skills leaf folder; criterion points there.
8. (B) status takeover: no unannounced criterion; if the first review is `ready`, the operator writes one real finding and hand-moves check.fix once.
9. (B) pull reconciles one file per number: delete on absent, rename on retitle.
10. (B) `akrogon config` on an uninitialized checkout prints global + defaults with `repo: none`.
11. (B) install has no test under AKROGON_HOME; init and config do.
12. (B) retire-old criterion 1 names the moved history files as byte-identical and exempt.

Rejected:
13. (A) Stale-call guard across revisited phases: a pane runs one turn and is re-prompted only after a phase move, so the `done` guard covers it; no expected-round argument. Stated in the design.

Slot B held four points after the rebuttal: diagnosis before the failed move rather than after; the hand-moved check.fix does not prove hands-off recovery; moved history files may still link into deleted folders; the `done` guard cannot tell an old completion from a new visit to the same phase. Slot A's answers are in the leaf designs; the last point is left to the command implementer if the live run shows a duplicate call.
