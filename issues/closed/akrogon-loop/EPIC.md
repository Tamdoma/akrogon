# Epic: akrogon-loop

Chart skill version: 4

The akrogon-new loop: after the two attended doors, two slots and one stateless command carry every leaf to merge with no operator action. Built by hand first, then by the tool on its own repo. Chart: issues/chart/akrogon-loop/.

## Issues

- `bootstrap`: Hand-built by the operator in two tabs with both slots guiding: the command and the five loop skills. Both leaves run in parallel.
- `takeover`: The first leaf built by the tool on its own repo. Proves the takeover: one hands-off merge plus one deliberate check.fix on a real failing check.
- `github`: The GitHub intake path: the pull mirror with closure on completion, and the seed-issue skill that colleagues' agents run. Two parallel leaves after takeover.
- `doors`: The attended doors: chart-issues rewritten as the one door, and init-issues for a new repo. Parallel leaves.
- `finish`: The last leaf: old material removed, old lessons moved to history, this repo indexed.
