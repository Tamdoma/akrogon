# Issue: takeover

Chart skill version: 4

The first leaf built by the tool on its own repo. Proves the takeover: one hands-off merge plus one deliberate check.fix on a real failing check.

## Leaves

- `status`: `akrogon status` and `akrogon status <slug>`, the notification on failed. First automated leaf, no debate.
