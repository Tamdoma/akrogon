# Implementation brief: chart-issues

## 1. Goal

Implement plan D1–D8 in the chart-issues worktree as one verifiable documentation unit. Replace obsolete intake workflows with one short chart door and three local assets. B performs independent fresh-reader and isolated handoff verification after the writer returns.

## 2. Numbered acceptance criteria

1. SKILL.md is below 300 lines, 4,000 tokens and 20 substantive rules, with compaction recovery, dependency, grounding, triggered references and footer.
2. Open/drain/decision/handoff implement plan D2–D6, including blind B, direct operator replies, provenance, drain stop, debate no, human prerequisites and authoritative-root writes.
3. Shapes yield current tree indexes and schema-valid state, exact source ownership, collision and missing-dependency refusal.
4. Three absorbed skill folders and chart fixtures/scripts/tests are deleted, owned references resolve, legacy source files are preserved.
5. Standing block stays verbatim with an interpretation of current behavior. Plan L1 remains explicitly reported, not concealed as a passing criterion.

## 3. Read-first list

Read this leaf's plan.md and its listed owned surfaces, authoritative brief.md/design.md, and `/home/ivan/.codex/skills/implement-issue/ponytail.md`. Copy the compact family shape in skills/plan-issue/SKILL.md. Inspect src/state.ts, config.ts, routing.ts and pull.ts for literal interfaces. Do not read lessons or the peer planning artifacts.

## 4. Change list and needed interfaces

Write skills/chart-issues/SKILL.md and assets/questions.md, shapes.md, standing-design.md. Remove superseded assets and skills/chart-issues/{fixtures,scripts,tests}, skills/{create-issue,braindump-issues,consolidate-issues}. Current state contains slug, phase, created, priority, repo, debate, blocked-by, sources, hand_built only when true. Debate no starts plan.synthesis, yes starts plan.positions. No executable source changes.

## 5. Do-not, reasons and exceptions

Do not edit command code, tests, unrelated skills, repository docs, lessons, installed roots or locked inputs because this leaf owns only the named skill rewrite/deletions. No new prose test framework or permanent fixture duplicates deleted machinery. Do not commit because B verifies and commits the whole result. Return a mismatch with concrete evidence instead of widening scope or changing interfaces. A revised brief from B is the exception authorizing a correction. Preserve L1 rather than changing the locked text. These exclusions protect ownership and independent verification, with only B's revised brief authorizing an exception.

## 6. Ordered steps

1. Before editing, note concrete failing old behaviors against criteria 1–4 as red evidence.
2. Rewrite SKILL.md and three assets around plan D1–D7. Remove superseded files/folders per D8.
3. Inspect resulting links, literal state example and workflow scenarios. Count substantive rules by meaning, not modal keywords. Run the targeted check below and fill the report.

Advisory size: four written files plus scoped deletions, under twelve turns. Return a mismatch if the scope materially exceeds that estimate.

## 7. Commands

No changed-prose runner exists. Targeted existing contract check: `AKROGON_BASE=398761c2afa453be5b383397c457c78c2066cb3a bun test tests/next.test.ts -t 'next creates one worktree/tab'`. This checks the real CLI/fake-herdr boundary used by the new state shapes, not prose correctness. B runs the full suite and remaining blocking checks after verification. Record `wc -l skills/chart-issues/SKILL.md` and reader evidence for the prose criteria.

## 8. Done-when, evidence and report

Return changed paths/reasons, targeted command output, concrete old/new workflow evidence, semantic rule count, limitations and unverified criteria. B separately arranges fresh reading, tokenizer measurement and a real sandbox handoff. Do not claim those passed before they run. All scenarios use temp repos and real processes with herdr/gh substituted, never real panes, GitHub, install roots or sockets.

Changed files and reasons: Replaced skills/chart-issues/SKILL.md with the current open/drain/decide/handoff flow; added assets/questions.md for operator batches and blind file exchanges and assets/shapes.md for provenance and current chart/container/leaf contracts; appended the current interpretation to assets/standing-design.md while retaining the original block byte-for-byte. Deleted the three absorbed skill folders, chart fixtures/scripts/tests and superseded question-authoring/materialization-contract/seed-shapes assets. No command or test source changed and no commit made. Parent correction removed L1 bookkeeping from the reusable asset; this report retains the acceptance limitation. effectiveConfig in src/config.ts emits the literal repo string none for unregistered directories, and shapes now explicitly identifies that value.

Old/new workflow evidence: Old materialization-contract emitted phase consult-position and a flat leaf folder without debate, blocked-by or sources. The new literal YAML parses through src/state.ts stateSchema as plan.synthesis/debate no with empty dependency/source arrays. Old chart handoff moved charts and emitted proposal/series machinery; new shapes retain charts and reports, index immediate epic/issue children and refuse missing prerequisites or occupied partial folders before handoff writes. New decision text specifies blind opening and decision passes, one disagreement-only rebuttal, direct B replies and a focused late-contract check. A drain stops even for one unresolved destination; a settled direct item writes chart records then hands off.

Size and reader evidence: wc -l reports 62 lines. Writer semantic count is 19 behaviors: recovery/reference loading; four grounding habits; root/pane context; refresh failure handling; grounding/lessons resources; provenance; map; split; drain/direct routing; blind decision exchange; resolution/specifiability; human prerequisites; handoff election; preflight/validation; retention/no dispatch; footer. This counts related conditions as one workflow duty rather than modal words and awaits independent reader assessment. All literal owned Markdown links resolve; exactly SKILL.md and the three intended assets remain.

Outside ownership: references remain in README.md, docs/guide.html, new-beginning/SESSION-SUMMARY.md, new-beginning/README-jul28.md, reference/akrogon-config.yaml, reference/lessons/2026-09-05-up-duplicate-panes.md and historical learnings/charting files. They were reported rather than edited. A repository-wide reference search exposed historical lesson matches incidentally; no lesson resource was opened as implementation guidance.
Tests run: AKROGON_BASE=398761c2afa453be5b383397c457c78c2066cb3a bun test tests/next.test.ts -t 'next creates one worktree/tab' passed: 1 pass, 14 filtered, 0 fail, 11 expect calls. Printed prompt: plan-issue build slot=B phase=plan.synthesis. Literal YAML schema parse passed with command-owned defaults supplied by stateSchema. Initial schema-check shell quoting accidentally treated Markdown backticks as command substitution and failed; rerun with shell-safe quoting passed. No files were changed by that failed probe.
Known limitations: L1 historical standing quotation conflicts with strict brief criterion 5.
Unverified criteria at writer return: Fresh-reader scenarios, tokenizer count, real isolated handoff and full checks were pending B. They are completed in the final report below.

### B final report

Changed files and reasons: Commit 308d5ed rewrites the chart door and three references and deletes the three absorbed skill folders plus obsolete chart machinery. 43 files changed, 235 insertions and 4,035 deletions. No unrelated source changes. Authoritative evidence is saved in this implementation folder.
Tests run: 46 tests passed with 487 assertions, format and typecheck passed, and diff whitespace check passed. Fresh-reader scenarios and actual isolated handoff/status/next passed for both initial phases. Preflight refusals preserved complete snapshots, and the real CLI rejected externally malformed missing dependency state. Main skill is 62 lines, 1,120 o200k_base tokens, with 17 coherent constraints in the independent reader's judgment. See verification.md, fresh-reader.md and adjacent JSON output.
Known limitations: L1 remains: the explicitly locked historical standing quotation prevents literal satisfaction of brief criterion 5 despite the current interpretation. General mixed per-issue debate choices are not expanded beyond the tiny exemption and one election. Out-of-scope reference paths are reported in verification.md.
Unverified criteria: Literal removal of the L1 quotation is intentionally not achieved under the verbatim lock. All planned executable and fresh-reader checks ran. Temporary scripts, dependency project and sandboxes were deleted after evidence capture. Worktree is clean and commit is ahead of AKROGON_BASE before review.
