# Fresh reader verification

Read only the chart skill and its questions, shapes and standing-design assets for workflow guidance. Read tests/helpers.ts, tests/fake-herdr.ts and tests/next.test.ts only to operate the isolated command boundary. No planning artifacts, implementation changes, real Herdr, GitHub, installations or sockets were used. Re-read standing-design after its bookkeeping paragraph was removed.

## Expected scenarios

1. **F1: Tiny direct request, no B.** Announce single slot. Resolve registered root through config, refresh intake and inspect grounding. Present a proportional map. When fully specified, create `issues/chart/<destination>/{CHART.md,INTAKE.md,decisions/}` and proceed directly to the concrete handoff. Tiny uses debate=no, phase=plan.synthesis without a debate question. Honor existing concrete authorization. Write `issues/open/<issue>/ISSUE.md` and each leaf's brief/design/state, validate status, retain chart with dated handoff marker and stop before dispatch.
2. **F2: Five reports, one unresolved destination.** Preserve reports and copy their full text into provenance-bearing INTAKE. Propose grouping before writes, create the one destination chart, then stop for chart selection even though only one resulted. Do not treat a seed drain as the settled direct-item shortcut.
3. **F3: Multiple destinations and duplicate imports.** Deduplicate exact mirror Source identities within the batch and against parsed open/closed leaf sources and every chart intake, including handed-off charts. Skip the already-imported legacy repo-relative provenance path. Body mentions do not count as provenance. Keep source originals unchanged. Create one chart per destination and stop for selection. One GitHub identity gets one issue/epic completion owner and belongs on every descendant leaf, never unrelated owners. Legacy paths never enter leaf sources.
4. **F4: Named B and late correction.** Use independent opening maps in distinct temporary files, then transfer useful findings and remove temporary files. A interviews and records. B receives intake, current Question/carries, related paths, locks and verbatim corrections, never A's draft/current Findings. Under chart/slots, retain independent batches, attributed merge and one disagreement-only rebuttal. Present all material questions plus challenge check. A late mechanism/contract change requires B's focused final check before recording, while a restatement does not. B answers direct operator requests in B's pane. Peer file existence is not readiness: use supported idle waiting, prompt, then timeout-free agent wait.
5. **F5: Missing dependency or occupied partial leaf.** Refuse all handoff writes before emission. A folder with a sentinel brief and no state is still occupied. A blocked-by target must resolve to a valid existing leaf or proposed prerequisite. Reject cycles and duplicate leaf slugs too. Emit prerequisites first after a valid complete audit.
6. **F6: Unregistered repo.** Continue local charting and report registration/refresh/grounding gaps. Do not initialize/register or emit leaf contracts through this door. Handoff waits for a registered destination.

## Findings and limits

7. **F7: Rule-budget judgment depends on granularity, not a missing behavior.** Counting every required protocol action separately produces more than twenty. Counting coherent behavioral constraints produces seventeen: grounding, authoritative destination, intake freshness, provenance conservation, proportional mapping, outcome grouping, chart lifecycle, independent peer exchange, operator-owned decisions, measurement consent, completed human prerequisites, self-contained contracts, debate election, authorization reuse, implementer preflight, schema-valid emission and dispatch ownership. Extraction/deduplication/copying are one provenance constraint; waits/files/rebuttal are the peer-exchange protocol. Under this defensible semantic grouping the main skill meets the cap. I found no concrete missing behavior or necessary wording correction. A count based only on prose paragraphs would be insufficient, and the precise semantic total remains judgment-dependent.
8. **F8: Debate scope has mild ambiguity.** “One door election” does not expressly define mixed issue choices. This fixture needs no invented general mixed-choice behavior: the tiny identity issue takes the explicit no-question/no-debate exemption, and the parser issue uses the authorized yes choice. Naming B alone never elects implementation debate.
9. **F9: Known standing-text conflict remains.** The required verbatim block still says the operator-owned chunk “parks at dispatch before any seat spawns.” The interpretation expressly disallows using that historical sentence as a hold mechanism and requires known physical prerequisites completed before opening a leaf. Behavior is clear, but a prohibition against retired dispatch references cannot also be literally satisfied while preserving this text lock. The removed L1 bookkeeping paragraph is absent from the final asset.

## Executed handoff evidence

Sandbox: `/tmp/akrogon-tEQXC9/repo`, registered key `repo`, home `/tmp/akrogon-tEQXC9`. Real CLI: the worktree's `src/akrogon.ts` through Bun. Fake executable: `/tmp/akrogon-tEQXC9/bin/herdr`, database `/tmp/akrogon-tEQXC9/herdr.json`.

10. **F10: Preflight refusals passed.** Prepared a source original and occupied `issues/open/occupied/partial/brief.md` sentinel. Audited a proposal naming missing `absent`, refused it without writes, then independently refused the partial-leaf destination. SHA-256 snapshots of every issues file remained identical after each refusal. These are the reader's actual pre-write decisions, not an invented claim that status implements the door's preflight. Evidence: `/home/ivan/Work/infra/akrogon/issues/open/akrogon-loop/doors/chart-issues/implementation/refusals.json`.
11. **F11: Valid direct emission passed.** Created chart `issues/chart/utility-suite`, a decision, intake with exact copied source text, and `issues/open/utility-suite/EPIC.md`. Child `identity/ISSUE.md` indexes `emit-identity`; child `numbers/ISSUE.md` indexes `parse-integer`. Each leaf has self-contained brief/design/state and the standing block plus interpretation. Interfaces, invalid-input expectations, exclusions and evidence-producing commands are explicit. Both dependency lists are empty because outcomes are independent. All proposed destinations/slugs were free before writes. Source `team/project#12` has the epic as completion owner and appears on both leaves. Original source and sentinel bytes survived.
12. **F12: Schema and dispatch passed.** Real status exited 0 before handoff marking and after dispatch. Initial states were emit-identity plan.synthesis/debate=no and parse-integer plan.positions/debate=yes. Appended `Handed off 2026-09-11` only after initial status success, printed the required stop footer, and then separately ran the explicitly authorized command-boundary verification. Both next invocations exited 0 and fake Herdr recorded exactly:

```text
plan-issue emit-identity slot=B phase=plan.synthesis
plan-issue parse-integer slot=A phase=plan.positions
plan-issue parse-integer slot=B phase=plan.positions
```

Chart, intake, copied report and original reports remain in place. Chart has no lifecycle state. Evidence files: `/home/ivan/Work/infra/akrogon/issues/open/akrogon-loop/doors/chart-issues/implementation/status-before.json`, `/home/ivan/Work/infra/akrogon/issues/open/akrogon-loop/doors/chart-issues/implementation/dispatch.json`; reproducible temporary helper `/tmp/chart-verification-pRhQ3W/verify.py`. The helper has already emitted contracts and deliberately will refuse rerunning into its occupied outputs. Sandbox and temporary evidence remain for parent inspection/cleanup. No skill files were edited and nothing was committed.

13. **F13: Actual command rejection passed.** Separately injected an externally malformed leaf with `blocked-by: [missing]`, then ran real `next broken-dependency` through fake Herdr. It exited 1 with `error: Missing leaf: missing`. Removed only that invalid fixture container afterward. Full output is retained in `/home/ivan/Work/infra/akrogon/issues/open/akrogon-loop/doors/chart-issues/implementation/missing-dispatch.json`. Successful contracts, source original, sentinel and chart remain.

Parent inspection and cleanup: B inspected the actual emitted indexes, states, chart and command outputs. Command/refusal JSON was copied beside this report. The temporary helper, tokenizer project and sandbox were removed after inspection. Sandbox paths above identify the executed case, not retained deliverables.
