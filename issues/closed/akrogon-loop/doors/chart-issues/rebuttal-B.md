# Rebuttal B: chart-issues

Read both independent positions against the locked brief/design and live command. Slot A's rebuttal was not read. The rewrite, deletions, independent B passes, source ownership, current state phases and isolated acceptance exercise are agreed. The following resolves the substantive differences without reopening scope.

## D1 and D5. Accept two assets, retain input provenance

Accept A's `assets/questions.md` and `assets/shapes.md` instead of B's four retained filenames. Asset count is not a behavioral fork. The shapes reference still needs the incoming GitHub `Source: owner/repo#n` identity, legacy proposal provenance and preservation of imported text. Eliminating seed-output mode must not eliminate seed-input guidance.

The existing leaf's creation-locked standing lines remain untouched and verbatim. For the rewritten output asset, copying the obsolete dispatch sentence verbatim would preserve a behavior the brief explicitly removes: it says an operator-owned chunk “parks at dispatch.” The brief requires replacement standing-design shapes with no reference to that mechanism, and Door Settles Human Steps requires known human steps completed before a leaf opens. Preserve the security and real-verification constraints in the new template, express human prerequisites using that current rule, and leave this leaf's historical binding quotation unchanged. This is a distinction between rewriting the owned asset and editing the locked input design, not permission to discard standing constraints.

## D2. A drain stops even when grouping produces one chart

Retain B's distinction. The brief says the drain writes one chart per destination “then stops for the operator to pick one,” and criterion 2 repeats the stop. A's “One destination: continue” applies that exception too broadly.

Concrete case: five reports consolidate to one destination with three unresolved choices. Write the chart and stop. A direct single fuzzy issue whose map has no unspecified work writes its chart and proceeds to handoff, as One Door Skill explicitly permits. The trigger is settled direct work, not the count of output folders.

## D2. Do not delete imported legacy seeds

Reject A's handoff deletion of `issues/open/<slug>.md`. The brief authorizes reading and copying reports and deleting named skill folders. It does not authorize deleting source reports. A already proposes the sufficient mechanism: retain the source path in intake provenance and skip it on later drains. Use that before and after handoff because the chart stays in place. GitHub sources remain GitHub identities, never local paths.

Concrete case: a legacy report is imported, handed off and drained again. Its recorded path suppresses reimport, and the original remains available. No extra file mutation is needed.

## D4. Correct B's debate default to no

Concede B's `default yes` as a defect. The operator correction dated 2026-09-10 in Debate Count explicitly says `default no`, overriding the earlier default. Accept A's one election informed by the settled design. Very small issues skip the question and use no. No new debate is needed over this correction.

## D4. Resolve worktrees to the authoritative root

Reject A's requirement that cwd be the registered checkout rather than a worktree. `src/config.ts:currentRepo` resolves a checkout through its Git common directory, so worktrees are intentionally accepted. `src/pull.ts:pullRepo` then reads origin and writes seeds at `repo.root`. B's current planning pass itself is running in such a worktree.

Permit chart invocation from a recognized worktree and write authoritative chart/handoff files at the configured root. If unregistered, report the gap and continue charting as the brief requires, with registration needed before dispatchable leaf materialization. Do not turn pull's registration requirement into a ban on opening the door.

The explicit nonblocking pull exceptions are unregistered and non-GitHub repositories. A's blanket continuation after every nonzero exit adds an unrequested stale-mirror fallback. An authentication or listing failure must remain a visible failed refresh, not silently change the drain to old contents. Independent operator notes can still be considered, but successful mirror intake cannot be claimed. No command change is required.

## D4. Slug uniqueness does not replace destination checks

Retain B's check for occupied destinations. `allLeaves` only recognizes folders holding state.yaml. A unique slug can still collide with a preexisting partial leaf containing a user-written brief. Index files can also exist independently of leaf slugs. A's proposal to remove destination preflight therefore permits data loss that its slug check cannot detect.

Keep this small: identify files/folders this handoff will create, reject occupied new destinations, and verify every dependency resolves to an existing or planned leaf before writing. Emit planned prerequisites before their dependents. No transaction system, new validator or extra operator approval is needed. `akrogon status` is useful schema evidence, but its scan of open state does not prove all destination and dependency checks.

## A1–A3. Verification agreement and remaining limits

Use A's two-asset layout with the shared fresh-reader and temporary-repo exercises. Include a one-destination drain with unresolved decisions, repeated legacy import, and an occupied partial leaf in the reader's scenarios so the disputed behavior is observable. Keep both initial planning phases and missing-dependency refusal in the real isolated handoff check. Sources can be absent for unsourced leaves under the schema, but the required GitHub-sourced handoff demonstration must include them.

The chart peer-answer directory name is not a substantive fork. Accept A's `slots/` convention with exact output paths and file-returned answers. The opening map can precede destination selection, so the skill must explain how its temporary answer files are located before chart folders exist, without treating answer files as chart selection or lifecycle state. A temporary directory outside the chart store, removed after retaining useful findings, is sufficient.

No production code, tests, locked inputs or peer artifacts changed in this pass. The configured index remains absent (`grounding: none`), and registration, repository documentation and external skill consumers remain outside this leaf. The synthesis can resolve the remaining presentation details directly from these constraints.
