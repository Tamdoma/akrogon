# Rebuttal A: chart-issues

Slot A, 2026-09-11, after reading `positions-B.md`. Only real forks are listed; everything else in B's position is compatible with A's and needs no debate.

## Agreed without change

Single SKILL.md with Open, Drain, Decide, Handoff and the footer; four Pocock lines; `akrogon` named as the dependency; blind B input (intake, Question and carries, related paths, locks, verbatim corrections; never A's draft or Findings); one disagreement-only rebuttal shown under the challenge check; focused B check on a late mechanism, none on a restatement; chart folder written even for the single fuzzy issue; two or more destinations stop, one continues; split shown before writing; slug uniqueness across open and closed; refuse a `blocked-by` naming a folder neither existing nor written by this handoff; validate the whole set before the first write; peer returns as files, never pane reads; `Next: none` at the door; delete the three skills and `fixtures/`, `scripts/`, `tests/`; report out-of-scope references in README, `reference/` and `new-beginning/` without editing them; fresh-reader scenarios plus a real handoff on a temp repo with fake herdr as evidence.

## F1 Debate default

B: default `yes`. A: default `no`. The design's Debate Count section, operator 2026-09-10 F1-A: "`debate: yes|no` is the whole election, asked once at the door with a recommendation from the settled design, default no". B's default contradicts the lock. Resolution: default `no`, recommendation stated per handoff, very small issues skip the question and get `no`.

## F2 Standing design lines

B (D4): "replacing obsolete chunk/gate/parking language with the current worker, checker and human-step behavior". The design's reading note of 2026-09-10 says the standing lines "are the operator's creation-locked text and stay verbatim", and the note itself carries the translation. Resolution: the lines stay byte-identical; the asset carries them plus the reading note; the skill never rewrites them.

## F3 Asset set

B: four assets, including `seed-shapes.md` rewritten as input examples. A: two, questions and shapes, with standing lines inside shapes. The design's Leaf architecture names three topics: question authoring, leaf shapes, standing design. Resolution: three files, `questions.md`, `shapes.md`, `standing-design.md`. Keeping the standing lines as their own file makes the byte-identical copy trivial, so A concedes that split. A `seed-shapes.md` for inputs is dropped: the mirror shape is written by `src/pull.ts` and read directly, the legacy proposal is dead intake, and one sentence in Drain ("identity is the `Source: owner/repo#n` line; a legacy file's identity is its path") covers both. A fourth file adds a trigger with no rule of its own.

## F4 Legacy seed files after handoff

B: keep the path as provenance in INTAKE.md. A: also delete the `issues/open/<slug>.md` file once its text is copied verbatim into INTAKE.md at handoff. Without deletion the file re-drains at every open and the skip rule needs a second key (path in an intake) forever. Deletion at handoff is one step and leaves `issues/open` holding only leaf trees. Resolution: both. The intake heading names the path as provenance, and handoff deletes the file. B's point that no fabricated GitHub source is written for it stands and matches A's R5.

## F5 `sources` when nothing is GitHub-sourced

B: always write `sources`, `[]` when none. A: omit when none. The schema accepts both, and criterion 3 lists `sources` among the fields the dry-run `state.yaml` must have. B's reading satisfies the criterion literally at no cost. A concedes: always write `sources`.

## Not forks

B's `questions/<id>.md` under the chart against A's `slots/<name>.md` is naming; synthesis picks one and the prompt to B states the exact path every time. B's "emit prerequisites before dependents" and A's "validate the whole set, then write" are the same rule stated from two ends. B's `readState` parse and A's `akrogon status` run are the same schema check through two entry points; the CLI run is the one a skill can name.
