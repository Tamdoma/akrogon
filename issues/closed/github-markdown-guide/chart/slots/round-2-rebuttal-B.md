1. Q4's requirement that **every command, flag, phase, config key and path exists in `src/`, `config.yaml` or `skills/`** cannot cover the selected guide. The new gacp function comes from `/home/ivan/.local/bin/gacp:4-19`, and its `.bashrc` installation is explicitly required by `forks/round-1.md` Taken. Illustrative project paths and issue slugs also need not exist in this checkout (`docs/guide/create.html:67-83`, `setup.html:57`). Require Akrogon behavior to match its implementation, external commands to match their own primary documentation or inspected source, and example paths/placeholders to be clearly identified and internally consistent. Judge the instructions' correctness, not literal presence in three repository surfaces.
