1. **F1 — Conflicting authority rules.** Brief criterion 3 says to report conflicts between code, config and skills “instead of resolving” them. Design line 25 directs the page to follow code when they conflict. Those prescribe different outcomes. State one policy in both contracts: either document current code behavior and report the conflicting instruction, or leave the disputed guidance pending an operator decision.

2. **F2 — The required gacp comparison introduces an inaccurate sync claim.** Design line 66 says sync “refuses off-branch or staged states.” `src/sync.ts:17-33` refuses staged paths outside eligible issue records, not every staged state. This contradicts brief criterion 3's accuracy requirement. Specify “refuses the wrong branch or staged paths outside eligible issue records.” Also qualify “commit everything” with the already-recorded distinction: `git add .` stages the current directory subtree, while the commit includes the whole staged index (`/home/ivan/.local/bin/gacp:9-10`).

3. **F3 — Recovery is not to any phase.** Design line 39 says failed routes to “any phase,” but its cited `src/routing.ts:35-38` lists only the seven active phases, excluding `merged` and `failed`. Say “any active phase listed in routing,” subject to the phase command's checks. Otherwise fulfilling the mandatory correction list would retain incorrect recovery guidance.

4. **F4 — The dispatch evidence omits the cited checks.** Design line 34 attributes `done` and `prompted` checks to `src/next.ts:512-542`. Those checks are at `src/next.ts:408-419`; the supplied range covers leaf-level dispatch and dependencies. Cite both ranges so the implementer can substantiate the required correction.
