1. The destination promises “the same 17 stems plus gacp.md,” but the recorded layout absorbs `index.html` into README (`CHART.md`, Destination; `forks/round-1.md`, Findings). Those are different outputs. Specify the 16 non-index topic pages plus `gacp.md`, with the homepage content in root README.

2. The `.bashrc` adaptation must isolate the script's shell behavior. `/home/ivan/.local/bin/gacp:2` sets shell options and lines 7 and 17 call `exit 1`. Wrapping those statements in a normal `gacp() { ...; }` function changes the caller's shell options and can close the interactive shell on refusal or failure. Specify a subshell-bodied function, `gacp() ( ... )`, retaining the script body inside it. This preserves the executable's isolation without adding a repository script or changing its Git workflow.
