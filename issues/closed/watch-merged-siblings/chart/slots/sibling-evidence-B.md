This round chooses the smallest way to stop treating a merged leaf waiting on siblings as an error. The operator asks: “1 - is there an even more elegant and minimalistic solution? It's okay if not.” A wording-only change can meet the seed's no-unnecessary-next requirement without changing the observer's output (`issues/chart/watch-merged-siblings/INTAKE.md:31-32`; `skills/watch-issues/scripts/observe.ts:218-230`).

### Q1 · Should the watch read sibling state only when a merged leaf remains open, rather than add owner evidence to every observation?

A merged leaf may legitimately remain open until its whole owner finishes. The owner is the top-level issue or epic, including leaves of other issues inside that epic (`src/phase.ts:140-147,170-172`). The current rule mistakes remaining under open for an error, even though the command has a separate error-handling rule (`skills/watch-issues/SKILL.md:37,41`).

Research: operator · `issues/chart/watch-merged-siblings/INTAKE.md:31-39`, read 2026-09-21, plus the operator's direct question quoted above · the requested result is no next call for a merged leaf waiting on siblings, with minimum change · prefer a conditional read in skill prose. better-than-training · `src/phase.ts:114-126,138-172`, read 2026-09-21 · merge records state before attempting owner completion, and completion can return normally for unfinished siblings or throw before moving the owner · retain a recovery path for completion failures, not an unconditional error report. better-than-training · `src/next.ts:525-527,548-550,564-573,664-672,710`, read 2026-09-21 · a merged dispatch returns completed even when its owner stays open, then triggers a sweep across registered repos; reported skips make the invocation fail · command results can identify actual command errors, but calling next is not a harmless sibling probe. better-than-training · `skills/watch-issues/scripts/observe.ts:167-175,218-230` and `skills/watch-issues/SKILL.md:30,41,53`, read 2026-09-21 · observation already reads leaf paths internally but does not print owner identity; read-only inspection and command-error handling already exist · no observer field or new error rule is required.

- **A (recommended)** Change only the merged rule: for a merged leaf still under open, read the leaf states beneath its top-level owner. If any is not merged, report waiting on siblings and do not call next for that merged leaf. If all are merged, run `akrogon next <slug>` once for that owner this fire, re-observe, and apply the existing command-error rule to an actual failure. Never infer an error just from the folder remaining open. Cost: a targeted tree read when this case occurs, but no new output contract. This matches the owner condition and the seed's requested behavior (`src/phase.ts:140-147`; `issues/chart/watch-merged-siblings/INTAKE.md:32`; `skills/watch-issues/SKILL.md:41`).
- **B** Keep the existing next call and remove only “if the leaf stays, report the completion error.” Let the command-error rule report actual failures, and describe a successful call with a remaining leaf as pending owner completion. This is the shortest edit and needs no sibling evidence. Cost: it keeps repeated calls while siblings work, does not meet the seed's no-next condition, and each completed dispatch can initiate a global sweep. A nonzero result may concern another leaf in that sweep, so it cannot automatically be labeled this leaf's completion error (`src/next.ts:525-527,564-573,664-672,710`; `skills/watch-issues/SKILL.md:41`).
- **C** Add `owner=` to each observation line and group leaves by owner before applying A's decision. Cost: changing the stable output format, observer implementation and its validation rather than only the Judge wording. The observer already holds paths internally, so this is feasible, but an owner field alone still needs grouping to determine whether every sibling merged (`skills/watch-issues/SKILL.md:26-28`; `skills/watch-issues/scripts/observe.ts:167-175,218-230`).
- **D** Remove the merged rule entirely. Normal merge already attempts completion, so this removes unnecessary probes. Cost: it also removes the watch's targeted recovery for a state saved as merged before completion fails. For example, an existing destination throws after merged state has been saved, and phase cannot be repeated on that terminal state; next supplies the completion retry (`src/phase.ts:114,126,148,185-186`; `src/next.ts:525-527`).

Suggested wording for A:

> Merged still under open: read the leaf states beneath its top-level owner folder under issues/open, an issue or epic. If any is not merged, report waiting on siblings and do not run next for this merged leaf. If all are merged, run akrogon next <slug> once for that owner this fire, then re-observe. Use the command-error rule for a failed command; remaining under open alone is not an error. If sibling state cannot be read, report that gap rather than assume completion.

Pitfalls: Do not check only the immediate issue inside an epic, or count only active siblings: any non-merged sibling prevents owner completion (`src/phase.ts:140-147`). After next, use fresh observation before another action because the call can move the owner and sweep other leaves; on command error, the existing rule already prohibits further mutation that fire (`src/phase.ts:172`; `src/next.ts:672`; `skills/watch-issues/SKILL.md:41`).

Reply `1-A`, `1-B`, `1-C`, or `1-D`, or give a numbered free-text answer.

Challenge check

B is more minimal by word count, and it correctly removes the false inference without sibling evidence. I recommend A because the requested behavior also avoids unnecessary next calls, whose global sweep makes “harmless no-op” an incomplete description (`issues/chart/watch-merged-siblings/INTAKE.md:32,35`; `src/next.ts:564-573,672`). D relies too heavily on successful completion at merge: the saved-state-before-completion ordering leaves a real retry case (`src/phase.ts:114-126,148`). Keep the implementation to skill wording; the guide describes the watch generally and does not promise the faulty merged-leaf behavior, so its existing description needs no change for A (`docs/guide/in-practice.md:79-91`). No watch edits to issues or state are needed or permitted (`skills/watch-issues/SKILL.md:53`).
