1. The challenge check says the automation lock forbids option B's existing sweep (`issues/chart/watch-merged-siblings/slots/sibling-evidence-merged.md:16`). The operator's lock is no automation **beyond what exists**. The existing watch already calls next for merged leaves (`skills/watch-issues/SKILL.md:37`), and that existing call triggers the sweep (`src/next.ts:525-527,664-672`). B preserves that behavior rather than adding automation. Reject B because it retains unnecessary calls and fails the seed's no-next expectation (`issues/chart/watch-merged-siblings/INTAKE.md:32`), not because the lock forbids it. A also retains the same sweep when it retries a fully merged owner.

2. The observer citation is wrong (`issues/chart/watch-merged-siblings/slots/sibling-evidence-merged.md:5`). `skills/watch-issues/scripts/observe.ts:108` is inside repository configuration resolution. The output without an owner field is constructed at `skills/watch-issues/scripts/observe.ts:227-230`. Replace the citation.
