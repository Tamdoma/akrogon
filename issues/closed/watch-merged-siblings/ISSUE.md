# Issue: watch-merged-siblings

- [merged-siblings-rule](merged-siblings-rule/brief.md): the watch reports a merged leaf with unmerged siblings as waiting and runs `next` only when its whole owner is merged.
