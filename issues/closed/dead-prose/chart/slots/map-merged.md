# Merged map: dead-prose

Sources: slots/map-A.md, slots/map-B.md. Tags: (A) only A raised it, (B) only B, (both) both.

## Agreed ground
- (both) No code reads the footer, questions folder, diagnosis paragraph, ponytail or LESSONS.md beyond init scaffolding; every item is agent-read prose.
- (B) The log records moves only (src/log.ts:16-31), so "never fired" for peer questions, lesson removals and diagnosis writes rests on the absence of artifacts in closed leaves, not on log rows.
- (B) merge-issue:37 is dead only once merge-conflict-route has merged; on origin/main today merge → check.fix can still reach the cap. The L4 leaf must name that leaf as a prerequisite rather than assume it.
- (both) The chart's own mandatory B exchange is a different mechanism from the five optional peer-question paragraphs and stays.

## Forks
1. (both) Peer-question paragraphs, five copies. Delete all, or keep plan-issue:27 (A).
2. (both) Footer: keep `Last operation:`, drop `Next:` and the scrambled paragraph, and fix the adjacent "two lines" and routing sentences in all eight skills (B: P2, R2). A also listed dropping the whole footer; B's R2 argues against.
3. (both) Failed diagnosis: A recommends deleting merge-issue:37 only and keeping check-issue:47 as the one written failure reason; B's P3/R3 require a retained record of why a leaf failed. B's F1: the deletion depends on merge-conflict-route.
4. (both) Ponytail: relative symlink from check-issue to implement-issue's file (Astra F7); installer links whole folders so it resolves (B: P4, R4).
5. (both) Lesson rule: delete the apply-removes-and-dates clause; B's P5/R5 keep creation, evidence and operator prune. A additionally proposed collapsing the four shape restatements into plan-issue:31.
6. (both) Lesson commit: both files in the registered checkout, operator commits, drop check-issue:41's leaf-branch clause; B's R6 names the worktree-dirty trap.
7. (both) Small rounds: drop "never cut"; keep question, evidence, options, recommendation with reason, explicit operator resolution and B's disagreements (B: P7, R7).
8. (both) Corrections as dated additions; B's P8/R8: the current binding answer must be identifiable, originals kept.
9. (B) Where open precise questions live once `## Forks open` goes: B recommends Fog as the single pending list (O1); A prefers fork files without `## Taken` as the inventory and Fog narrow (O2). B's R10: resume must still find every open fork.
10. (B) How standing rules reach execution once the verbatim block leaves design.md: B recommends translating applicable rules into leaf criteria (O3); A proposed a pointer plus the interpretation paragraph. B's R11: implement and check read design.md only. A: a pointer must name the installed path for consumer repos.
11. (both) State written last, prerequisites before dependents; sample phase line keeps no → synthesis, yes → positions (B: F3, P12, R12).
12. (A) Leaf split: L4 lifecycle skills and L5 chart skill as two leaves; no shared line.
13. (A) questions.md carries uncommitted local edits; the operator commits them before the L5 leaf opens or the leaf conflicts.
