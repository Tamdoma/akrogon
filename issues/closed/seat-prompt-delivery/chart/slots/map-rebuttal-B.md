1. **F1 — K1's general next-pass retry rule conflicts with its ambiguous-timeout exception.** A fresh idle observation is not evidence that the previous prompt was never accepted. Current code records `prompted` only on success (`src/next.ts:429-436`), and its idle guard checks that success marker (`:373-382`). Moving the resend to another event/tick without an outcome-specific gate still blindly resends a timed-out submission. Herdr's timeout describes observed status and state sequence, not receipt (`/home/ivan/Work/infra/herdrdev/herdr-patched/src/api/wait.rs:632-655`). Recommend next-pass automatic retry only for a definite pre-send rejection. An ambiguous submission must remain unresolved until receipt/nonreceipt is established or enter a visible delivery failure without automatic resend. The new delivery leaf must own this gate explicitly.

2. **F2 — “Input handlers ruled out” exceeds the evidence given.** The subagent handler requires a matching reply key and waiting child (`/home/ivan/.pi/agent/extensions/tamdoma-subagents/index.ts:177-189`), so excluding that handler for an ordinary check prompt is supported. The question handler consumes input when a pending batch accepts it (`/home/ivan/.pi/agent/extensions/tamdoma-request-user-input/index.ts:969-970`). Merely identifying that condition does not establish that the old live pi had no pending batch at the incident times. The merged map supplies no contemporaneous evidence of that absence. Keep this conditional hypothesis unless the historical pending state is established. A later successful probe does not rule out earlier transient interception.

3. **F3 — Increasing the deadline does more than change the error label.** The initial effect gate stays capped at five seconds (`/home/ivan/Work/infra/herdrdev/herdr-patched/src/api/wait.rs:232-246`), but after an observed lifecycle change Herdr waits for the requested status using the remaining overall timeout (`:269-291`). A larger deadline can therefore change success versus timeout when an intermediate state arrives before five seconds and working arrives later. Replace “it only changes what K3 records” with that distinction. It still provides no proof of nonreceipt on failure and does not justify retrying an ambiguous submission.
