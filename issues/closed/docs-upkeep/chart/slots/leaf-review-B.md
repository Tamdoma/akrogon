1. **F1: L1-design.md:21, criteria 4, 6 and 8: the schema-only ownership excludes a required production fix.** Removing the grounding default makes `repoSchema.parse({})` fail in `src/config.ts:123`. That is how effective configuration is built outside an initialized repository. The setup skill begins with that command (`skills/init-issues/SKILL.md:16`), so fresh setup breaks before it can propose grounding. `tests/config.test.ts:35` also explicitly requires this unregistered-repository behavior. Allow L1 to update `effectiveConfig` as well as the grounding schema, keeping machine-only configuration readable without accepting omitted grounding in a persisted repo config.

2. **F2: L1-design.md:21 and L1-brief.md criteria 7–8: passing the suite requires test files outside ownership.** The shared fixture writes a config without grounding at `tests/helpers.ts:22`. Other tests independently write omitted-grounding configs, including `tests/sync.test.ts:42`, `tests/pull.test.ts:171`, `tests/phase.test.ts:83,241,886,895` and `tests/next.test.ts:667,1367`. `tests/state.test.ts:119,143,202` directly parse an empty object. Owning only init/config tests cannot preserve the suite after criterion 6. Allow the minimal explicit-grounding fixture updates in those files. Keep omission as a deliberate negative test. Add repeat-init coverage for both a valid declared index and existing explicit none, with preservation assertions. The existing registered repos inspected already have explicit grounding, so there is no evidence that valid registrations need a migration.

3. **F3: L1-design.md:24, criteria 5 and 7: the prescribed stat check does not establish readability.** A regular file can have positive size while its contents cannot be read by the process. The interface only specifies `statSync`, file type and size. Also, a missing path throws from stat before the described custom error is reached. Require an actual readability check and translate the relevant filesystem failures into the promised path-bearing setup error before any mutation. Include unreadable-file coverage alongside missing, empty and directory cases, using a test that does not depend on the runner being unprivileged.

4. **F4: L2-design.md:23, criteria 1–2: the path/deletion contract misclassifies valid globs and generated files.** In pi-extensions, `docs/reference-index.md:81` names `tamdoma-subagents/*.test.ts`, which cannot pass literal `[ -e "$path" ]` even though matching tests exist. Lines 20 and 78 name `herdr-agent-state.ts` and `tamdoma-subagents/manifest.json`. Both exist in the registered checkout but are ignored and untracked, confirmed by `git check-ignore` and `git ls-files --error-unmatch`. The manifest is explicitly generated at index line 78 and ignored by `tamdoma-subagents/.gitignore:3`. They need not exist in the implementation worktree, and removing such a row cannot supply a commit that deleted its file. Define validation for literal targets versus globs and generated artifacts. Allow generated-only rows to be retargeted to tracked source or removed with that reason, rather than requiring a nonexistent deletion commit. Validate the final index against the leaf worktree so machine-local files cannot make a broken checkout map appear valid.
