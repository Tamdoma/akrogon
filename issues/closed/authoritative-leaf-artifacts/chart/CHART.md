# Chart: pass artifacts land in the registered checkout, not the worktree

## Destination
A seat that writes a pass artifact into the worktree copy of its leaf folder is stopped at the next `akrogon phase` move with a message naming the authoritative folder, and the dispatch prompt gives that folder so the seat has no reason to guess.

## Forks taken
- [Where pass artifacts land and where the mistake fails](forks/artifact-placement.md): issues-diff refusal at every worktree move with no autocorrect, absolute leaf folder in the dispatch prompt, no artifact-existence gate.

## Forks open
None.

## Fog
None.

## Off route
- Repairing boulevard-automation branch `worker-scaffold` (drop its `issues/` commits, move `plan.md` and implementation briefs to the authoritative folder). Operator step in that repo.
- Searching worktree copies from the debate gate or copying artifacts automatically.
- Resetting a leaf to `plan.positions` when its debate did run.

Handed off 2026-09-19 into `../../open/authoritative-leaf-artifacts/`.
