# Issue: chart-terminal-stage

- [terminal-stage-marker](terminal-stage-marker/brief.md): recognize `Closed` and `Held` alongside `Handed off` so `akrogon status --charts` stops reporting finished charts as `charting`.
