# Round A (blind) 2026-09-19

Q1 who ends: akrogon on the blocked event (recommended). Research tier 3: src/next.ts:369-385 dispatchSlot returns for a busy pane, blocked included, before the attempts cap; src/next.ts:604-607 only working events return early, so the blocked event already runs dispatch for the owning leaf; src/routing.ts:26-38 no phase moves to failed. Option B seat declares via a new command: adds a command and still needs the event path for a seat that asks in prose. Pitfall: required seat of the current phase only (tests/next.test.ts:1565-1596); `unknown` excluded; harness approval dialogs also read as blocked, pi runs `-a` so they should not occur for pi.
Q2 state: reuse failed with reason (recommended). Research tier 3: src/routing.ts:35-38 failed routes back to any phase; src/state.ts schema has no reason field; status prints failed distinctly. New state costs routing, status, tests and a second recovery path.
Q3 preserve: worktree, partial commits, phase the attempt was in, blocker text (the pane's blocked label from herdr if present), no requireClean; recovery is the operator running `akrogon phase <slug> <phase>` from failed, as today. Research: src/next.ts:509-515 cleanup keeps pane/tab/worktree; herdr:blocked event carries a label (tamdoma-request-user-input/index.ts:823-828).
Q4 tool removal: launch flag (recommended, and it needs no pi-extensions change). Research tier 3: `pi --help` 2026-09-19 lists `--exclude-tools, -xt <tools>` "Comma-separated denylist of tool names to disable. Applies to built-in, extension, and custom tools"; the extension README states the guidelines disappear when the tool is disabled. Harness template `pi --model {model} --thinking {effort} -a` (akrogon config harnesses.pi) gains `--exclude-tools request_user_input`. Only akrogon-launched panes get it; the operator's own charting pane is unaffected. Pitfall: verify at implement that --exclude-tools drops promptGuidelines (not just the tool); a reused pane launched earlier without the flag keeps the tool until relaunched; codex/claude harnesses have their own ask surfaces and get the skill rule only.
Q5 seat behaviour at a human-only blocker: end the pass, write the operator step under report.md known limitations, do not wait (recommended). Research: skills/implement-issue/SKILL.md:39 says wait; the report file already exists in the skill. The seat then idles, the pass ends, and the lifecycle sees an unfinished phase on the next event; no new mechanism. Cost of the alternative (seat calls a stop command): new command surface.
Q6 channel: Discord via the existing broadcast.discord.webhook_env list when configured, herdr always (recommended). Research tier 3: src/config.ts:46; ~/.config/akrogon/env holds the values; skills/broadcast-issue/SKILL.md:39 loud on missing. Option separate alerts list: one more config key for the same webhook. Pitfall: B notes the broadcast contract is team-facing; the operator decides whether failures belong in the same channel.
Q7 delivery: once on the event with the external-API retry rule, failure logged, status stays failed regardless (recommended). Outbox would need a later trigger, which the locks forbid.
Challenge: a practitioner could argue the flag alone is fragile across harnesses; the answer is the skill rule plus Q1, which makes any escaped question a visible failure within one event.
