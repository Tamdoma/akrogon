- **Q4 changes an explicit carry without settling it.** `forks/ask-tool-availability.md:10` says the same pane may run chart-issues. The concrete permitted scenario is an operator invoking chart-issues in an allocated execution pane after its pass ends, using the same Pi process. Launch-wide exclusion cannot make that process interactive for charting. I found no evidence that this occurred in the reported incident, so it does not justify a dynamic mechanism by itself. Resolve the contract explicitly: execution Pi processes stay noninteractive, and charting requires a separate or restarted interactive process. With that boundary, the documented launch flag is sufficient and the per-pass switch is unnecessary. The merged assertion that this case never occurs cannot silently replace the carry.

- **Q4's config-only recommendation omits existing execution processes.** `allocate()` reuses matching tabs and recorded panes (`src/next.ts:286-290,311-341`), and `dispatchSlot()` applies the harness template only when `pane.agent === null` (`src/next.ts:389-404`). An idle Pi process started before the template change therefore receives its next execution prompt with the question tool still enabled. Add an explicit rollout prerequisite to safely relaunch existing execution processes with the exclusion, preserving work and dealing with pending batches without answering them automatically. Otherwise limit the enforcement claim to newly launched agents. This requires a migration boundary, not a new per-pass capability mechanism.
