- **F1 — Missing closed-leaf cleanup negative test.** `foreign-leaf-summary/brief.md:4` and `design.md:6` promise that foreign leaves never enter merged-leaf cleanup, but the required tests cover planning leaves, dispatch and dependencies. `src/next.ts:113-116` scans closed leaves too, and `cleanupMerged()` can close a tab, forcibly remove a worktree and delete its branch (`src/next.ts:505-511`). Add a foreign `merged` leaf under `issues/closed/` with a real fixture worktree/branch and an associated fake tab. Run a cleanup-capable invocation and assert it is reported while its state, tab, worktree and branch remain intact. Byte-identical state files alone do not prove cleanup exclusion.

- **F2 — Missing summary-scope edge tests.** `foreign-leaf-summary/brief.md:11` tests repeated discovery of one contaminated repository within one invocation. It does not distinguish per-repository deduplication from a single invocation-wide suppression flag, or verify that a subsequent invocation reports again. The binding rule is one summary per repository per invocation (`design.md:5,8,14`), and current invocation state is created anew at `src/next.ts:604`. Add two contaminated registered repositories in a machine-wide run, then repeat the invocation. Assert one complete summary for each repository in each run and nonzero status both times. Execute outside registered checkouts because `--all` inside one registered repository deliberately selects only that repository (`src/next.ts:627-634`).
