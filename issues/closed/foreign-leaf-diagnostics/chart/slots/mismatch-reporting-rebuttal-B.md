- Q1 changes the agreed reporting unit and incorrectly labels it “both.” My round recommends one summary per affected repository per invocation, with paths paired with their stored keys (`slots/mismatch-reporting-B.md`, Q1-A). The intake requests “one actionable message” for a contaminated repository, and the fork asks “one aggregated line per repo per invocation” (`INTAKE.md`, Scope; `forks/mismatch-reporting.md`, Q1). One line per `(repo, stored key)` produces several summaries when a repository contains multiple foreign keys. Keep one repository summary containing all stored-key/path groups, or present per-key grouping as an unresolved operator choice. Add a multiple-stored-keys case asserting one mismatch summary for that repository.
