1. **Q2 overstates the cost of repo-specific flags.** “Flags would need per-repo templates and install scanning” is not established. Installation uses harness names, not launch arguments (`src/install.ts:51`), and launch already separates the harness kind from its argument list (`src/next.ts:218-225,437-444`). Additional flags for an existing registered harness would not inherently require installation scanning or replacing its template. Keep the recommendation to limit this leaf to harness/model/effort, but describe flags as extra configuration and argument-composition rules. Repo scanning would become a separate concern only if repositories could introduce harness kinds absent from the global registry.
