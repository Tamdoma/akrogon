# Slot B disagreements

1. **D1 / F13: A's recommendation treats a new behavior change as existing staleness.** The pre-lock `hooked` boolean chooses the routing branch. It does not choose the leaf dispatched by that branch. Today `src/next.ts:691` refreshes owners after acquiring the global lock, then independently checks duplicate owners (`:692`), returns for zero owners (`:693`), and chooses the current owner (`:694`). Keeping the branch decision stale does not make the owner decision stale too.

   Concrete counterexample: a bare pane-owned invocation finds X, then waits while another command parks X. The current second lookup finds no owner and returns without a skipped-leaf error. Reusing the first result calls `dispatchLeaf`, which reports the missing X at `src/next.ts:486` and causes exit 1 at `:700`. These are different observable outcomes for valid commands and valid state. If X remains readable but loses ownership, `dispatchLeaf` refreshes its state by slug (`:481`–`:492`) without rerunning `ownsPane` (`:163`), so it can act on the former owner. “A sweep would do this anyway” does not establish correctness for an invocation routed to one pane's owner.

   **Correction:** describe option (a) as accepting newly stale owner selection, including changed error/dispatch outcomes, rather than accepting only already-existing staleness. I retain the recommendation to omit F13 under the note's speed-fix exception unless the operator explicitly chooses that behavior change or authorizes a revised implementation. The note's unchanged-valid-input requirement is at `issues/AKROGON-AUDIT-FIXES.md:5`.

2. **D1 option (c): the merged text overstates the test conflict and misattributes my conclusion.** My map said that moving **all selection** under the lock breaks the pre-flock rejection tests. It did not establish that every single-lookup-under-lock arrangement necessarily breaks them. `tests/next.test.ts:1565` exercises explicit slug/path inputs. Those can retain pre-lock selection while a separately handled bare-hook route changes its lock placement. `src/next.ts:648` already distinguishes explicit inputs from bare invocations.

   **Correction:** replace “which ... breaks tests ... (both reject)” with “requires a routing change; moving all selection under the lock would break explicit-invalid-target tests.” I reject widening this deletion-focused batch into a routing redesign without authorization. I do not claim a correct redesign is impossible or that the cited tests rule out every such design.
