# Chart: the command loses the machinery that never helped

## Destination
`akrogon next` and `akrogon phase` behave the same for every valid input with these gone: the stand-in seat and the `unknown` attempt jump, `recoverMerge`, per-repo `max_active`, the repo lock, and the hook path taken by a typed `next` inside a leaf pane. Two behaviours change: `failed` exits to a chosen phase by command, and a leaf branch with no diff against the target is refused at `check.review`. The `debate` key stays and gains one reader: dispatch refuses a `debate: yes` leaf at `plan.synthesis` without positions files. One code leaf in akrogon; `bun test`, typecheck and format green; no `issues/` diff on the branch.

## Forks taken
- [unknown status](forks/unknown-status.md): `unknown` waits like busy; stand-in code deleted.
- [failed exit](forks/failed-exit.md): every phase except `check.fix`, counters reset on exit.
- [debate key](forks/debate-key.md): key stays, dispatch refuses a debate leaf that skipped its debate.
- [repo lock](forks/repo-lock.md): deleted, `pull` unlocked.
- [guide ownership](forks/guide-ownership.md): the leaf deletes the guide and skill lines about deleted machinery.

## Forks open
None.

## Fog
None. Every item names its code site in INTAKE.md.

## Off route
- Busy notice stays; all three reviews kept it.
- Stalled seats belong to the pi-extensions loop-guard leaf.
- Merge conflict routing and what `fix_rounds` counts: L2 chart.
- `sync`, `park` taking a running leaf, `hand_built`: decision D1, not yet made.
- Docs consolidation (Astra F5) and an artifact-existence gate: not charted.
- Leaf locks: already deleted by the merged `audit-fixes` leaf.
- Missing positions files on `handoff-contract-slim` after a real debate: observed, not charted here.

Handed off 2026-09-14: issues/open/command-deletions/command-deletions-batch, phase plan.synthesis, debate no.
